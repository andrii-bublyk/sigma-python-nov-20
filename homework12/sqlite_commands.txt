1)
N/A

2)
N/A

3)
N/A

4)
CREATE TABLE "category" (
	"id"	INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT UNIQUE,
	"name"	TEXT NOT NULL
);

5) 
INSERT INTO category (name)
VALUES
	("book"),
	("food"),
	("dress");

6)
CREATE TABLE "item" (
	"id"	INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT UNIQUE,
	"name"	TEXT NOT NULL UNIQUE,
	"category"	INTEGER NOT NULL,
	"price"	NUMERIC
);

7)
INSERT INTO item (name, category, price)
VALUES
	("python for beginners", 1, 100),
	("c++ for beginners", 1, 100),
	("c# for beginners", 1, 100),
	("banana", 2, 100),
	("pineapple", 2, 100),
	("apple", 2, 100),
	("heat", 3, 100),
	("shirt", 3, 100);

8)
UPDATE item
SET price = 350
WHERE id = 1;

9)
UPDATE item
SET price = price *1.1;

10)
DELETE FROM item
WHERE id = 2;

11)
SELECT * FROM item
ORDER BY name;

12)
SELECT * FROM item
ORDER BY price DESC;

13)
SELECT * FROM item
ORDER BY price DESC
LIMIT 3;

14)
SELECT * FROM item
ORDER BY price
LIMIT 3;

15)
SELECT * FROM item
ORDER BY price
LIMIT 3, 3;

16)
SELECT name FROM item
WHERE price = (SELECT MAX(price) FROM item);

17)
SELECT name FROM item
WHERE price = (SELECT MIN(price) FROM item);

18)
SELECT COUNT(*) FROM item;

19)
SELECT AVG(price) FROM item;

20)
CREATE VIEW "The most expensive goods" AS
SELECT * FROM item
ORDER BY price DESC
LIMIT 3;
